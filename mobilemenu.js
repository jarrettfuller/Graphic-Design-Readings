        $(document).ready(function() {
            $('button.toggle').click(function() {
                $('header ul').slideToggle();
            });
        });

     /*   $('window').resize(funtion() {
            ww = $(window).width();
            if (ww > 800) {
                $('header ul').show();
            } else {
                $('header ul').hide();
            };
                               }); */
